import os
import shutil

def overwrite_folder(folder_path):
    # see if folder exists, if so delete it 
    # in either case create a new one
    print("complete me")

def write_to_file(string,path,overwrite=False):
    
    mode="a"
    if overwrite: mode="w"
    print("complete me")


